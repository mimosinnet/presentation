% layout 'default';
% title 'Has pujat un arxiu';
%=t h2 => 'Has pujat un arxiu'

%== $missatge

%=t h3 => 'Has carregat aquest arxiu'
%=t h4 => "$add_pres: $titol ($num_diapos)"  

% foreach my $line ( split /^/, $contingut) {
%==	"$line <br>" 
% }


%# vim: tabstop=2 shiftwidth=2
